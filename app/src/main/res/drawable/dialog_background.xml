<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradient background -->
    <gradient
        android:startColor="#FFFFFF"
    android:endColor="#F4F4F4"
    android:angle="270" />
    <!-- Vertical gradient -->

    <!-- Rounded corners -->
    <corners android:radius="16dp" />

    <!-- Border for neat separation -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" />

    <!-- Inner padding so content doesn’t touch the edges -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" />

</shape>
